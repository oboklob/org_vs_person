[build-system]
requires = ["setuptools>=45", "wheel", "setuptools_scm>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "name-classifier"
dynamic = ["version"]
description = "A machine learning-based name type classifier"
readme = "README.md"
requires-python = ">=3.8"
